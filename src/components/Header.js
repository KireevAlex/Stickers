import React from "react";
import Header_elem from "./Header_componends/Header_elem";

function Header({createNewSticker}) {
  
  return <Header_elem createNewSticker={createNewSticker}/>;
}

export default Header;
